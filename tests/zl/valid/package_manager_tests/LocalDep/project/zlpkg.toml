# Regression fixture - valid/package_manager_tests - LocalDep
#
# A project with one local path dependency, run through `zlpkg run` by the
# regression runner. Path dependencies resolve without any network access,
# which keeps this case hermetic.

[package]
name = "localdep"
version = "0.1.0"

[dependencies]
mathlib = { path = "../mathlib", version = "0.1.0" }
