# zlpkg.lock - generated by `zlpkg install`. Do not edit by hand.
# Run `zlpkg install --update` to re-resolve and rewrite this file.
#
# Deliberately stale fixture lock: it predates manifest fingerprinting
# (no [meta] section) and records no dependencies, while the manifest
# declares mathlib. zlpkg must re-resolve instead of trusting it.
